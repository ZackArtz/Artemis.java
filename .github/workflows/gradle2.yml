name: Gradle

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Gradle
      run: ./gradlew build
    - name: Docker Action
      uses: zenato/docker-action@v1.0.0
      with:
        username: zackartz
        password: zJYYttQGiV4CrG
        repository: zackartz/artemis
